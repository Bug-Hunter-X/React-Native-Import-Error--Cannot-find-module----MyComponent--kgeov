The corrected code includes the '.js' extension in the import statement.  This explicitly tells the import system to look for a JavaScript file.

```javascript
import MyComponent from './MyComponent.js';
```